require File.dirname(__FILE__) + '/spec_helper.rb'

describe Yr::Detail do
  before(:each) do
  end
  
  it "should render as string" do
    detail = Yr::Detail.new
    detail.temperature = 22.0
    detail.to_s.should == "Temperature 22.0"
  end

end